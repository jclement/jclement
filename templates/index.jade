extends layout

block header

block content
  div.jumbotron
    h1 jclement.ca
    p There is content here

  div.row
    div.col-md-6
      - var articles = env.helpers.getArticles(contents);
      h3 Articles:
      ul
        each article in articles
          li
            a(href=article.url)= article.title 
            = moment.utc(article.date).format(' (DD. MMMM YYYY)')
    div.col-md-6
      h3 Projects:
